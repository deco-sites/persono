import Component from "$store/components/ui/ExternalFrame.tsx";
import type { Props } from "$store/components/ui/ExternalFrame.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
