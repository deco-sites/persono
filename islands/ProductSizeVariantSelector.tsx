import Component from "$store/components/product/ProductSizeVariantSelector.tsx";
import type { Props } from "$store/components/product/ProductSizeVariantSelector.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
