import Component from "$store/components/product/SizeGuideModalButton.tsx";
import type { Props } from "$store/components/product/SizeGuideModalButton.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
