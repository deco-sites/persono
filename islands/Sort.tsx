import Component from "$store/components/search/Sort.tsx";
import type { Props } from "$store/components/search/Sort.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
